<main>
  <h1>{{ page.title }}</h1>
  {% if page.metafields.bannes.acf_bn %}
  <div>
    <img src="{{ page.metafields.bannes.acf_bn }}">
  </div>
  {% endif %}
  <div class="content">
    {{ page.content }}
  </div>
</main>